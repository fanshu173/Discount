import 'package:flutter/material.dart';

class MyFragment extends StatelessWidget {
    
  @override
  Widget build(BuildContext context) {
    return Row(
      children: [Text('MyFragment')],
    );
  }
}
